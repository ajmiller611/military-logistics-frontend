/**
 * CreateInventoryPage
 *
 * Serves as the form page for creating new inventory items.
 * Renders the CreateItemContainer component which manages
 * the form and API interactions.
 */
import CreateItemContainer from '@/components/inventory/CreateItemContainer';

export default function CreateInventoryPage() {
  return <CreateItemContainer />;
}
